{
  "name": "ci-failure-analyzer",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "adm-zip": "^0.5.16"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.0"
  },
  "scripts": {
    "build": "ncc build index.js -o dist"
  }
}
