{
  "name": "explain-ci-failure",
  "private": true,
  "scripts": {
    "build": "ncc build index.js -o dist"
  },
  "type": "commonjs",
  "version": "0.0.1",
  "dependencies": {
    "@actions/core": "^2.0.2",
    "@actions/github": "^7.0.0",
    "adm-zip": "^0.5.16"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
